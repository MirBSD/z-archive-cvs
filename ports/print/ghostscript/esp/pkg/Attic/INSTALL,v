head	1.2;
access;
symbols;
locks; strict;
comment	@# @;


1.2
date	2006.04.27.20.52.12;	author bsiegert;	state dead;
branches;
next	1.1;
commitid	10044512EA204B6306B;

1.1
date	2005.03.18.15.51.03;	author tg;	state Exp;
branches
	1.1.7.1;
next	;

1.1.7.1
date	2005.03.18.15.51.03;	author tg;	state Exp;
branches;
next	;


desc
@@


1.2
log
@Update to 8.15.2-0.

Fix MASTER_SITES. Use @@sample (I can't believe this thing still used an
INSTALL script). Regenerate patches and fix a png / zlib compat issue (I
should probably write a Problem Report to ESP about this).
@
text
@#!/bin/sh
# $MirOS: ports/print/ghostscript/esp/pkg/INSTALL,v 1.1 2005/03/18 15:51:03 tg Exp $
#
# Post-installation setup of espgs

PATH=/bin:/usr/bin:/sbin:/usr/sbin
PREFIX=${PKG_PREFIX:-/usr/local}

do_notice_etc_install()
{
    echo
    echo "+---------------"
    echo "| You appear to already have the file 'pstoraster.convs' in"
    echo "| ${SYSCONFDIR}/cups."
    echo "|"
    echo "| The configuration file has been preserved. If you want to"
    echo "| start from the default setup, you should perform these steps"
    echo "| as root:"
    echo "|"
    echo "|       rm -f ${SYSCONFDIR}/cups/pstoraster.convs"
    echo "|       cp -p ${PREFIX}/share/examples/cups/pstoraster.convs ${SYSCONFDIR}"
    echo "+---------------"
}

do_etc_install()
{
    cp -p ${PREFIX}/share/examples/cups/pstoraster.convs ${SYSCONFDIR}/cups
    echo
    echo "+---------------"
    echo "| The CUPS pstoraster configuration file has been installed in"
    echo "| ${SYSCONFDIR}/cups."
    echo "+---------------"
}



# verify proper execution
#
if [ $# -ne 2 ]; then
    echo "usage: $0 distname { PRE-INSTALL | POST-INSTALL }" >&2
    exit 1
fi

# Verify/process the command
#
case $2 in
    PRE-INSTALL)
        :
        ;;
    POST-INSTALL)
	if [ -f ${PREFIX}/share/examples/cups/pstoraster.convs ] ; then
	    if [ -f ${SYSCONFDIR}/cups/pstoraster.convs ]; then
		do_notice_etc_install
	    else
		do_etc_install
	    fi
	fi
	;;
    *)
        echo "usage: $0 distname { PRE-INSTALL | POST-INSTALL }" >&2
        exit 1
        ;;
esac

exit 0
@


1.1
log
@Initial revision
@
text
@d2 1
a2 1
# $MirOS$
@


1.1.7.1
log
@Import the MirPorts Framework, many files moved or renamed though, no KDE/QT
@
text
@@
