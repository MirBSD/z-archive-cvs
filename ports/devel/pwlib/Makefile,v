head	1.5;
access;
symbols;
locks; strict;
comment	@# @;


1.5
date	2008.10.16.19.43.49;	author tg;	state Exp;
branches;
next	1.4;
commitid	10048F798F722A050E5;

1.4
date	2008.04.07.19.20.04;	author tg;	state Exp;
branches;
next	1.3;
commitid	10047FA73D650DD59D5;

1.3
date	2006.09.14.00.08.49;	author tg;	state Exp;
branches;
next	1.2;
commitid	10045089C0E234154C3;

1.2
date	2006.02.09.11.11.27;	author tg;	state Exp;
branches;
next	1.1;
commitid	10043EB23217A35557B;

1.1
date	2005.03.18.15.45.24;	author tg;	state Exp;
branches
	1.1.7.1;
next	;

1.1.7.1
date	2005.03.18.15.45.24;	author tg;	state Exp;
branches;
next	;


desc
@@


1.5
log
@many more distfile fixes:
either change stuff to let “mmake checksum” succeed, for example by fixing
MASTER_SITES or DIST_SUBDIR, or by adding the “Size” That Does Matter™ to
the distinfo (so that b0rken downloads are skipped), or by whatever other
means, or mark b0rken stuff as BROKEN for sure. For squid, I removed one
of the upstream patches from the list.

After this, all non-BROKEN (and some BROKEN and NOT_FOR_PLATFORM) distfiles
verify successfully (and thusly link to FTP/ and CDROM/ on the mirror).
@
text
@# $MirOS: ports/devel/pwlib/Makefile,v 1.4 2008/04/07 19:20:04 tg Exp $

BROKEN=			unable to fetch distfile

COMMENT=		cross platform C++ socket and stuff library
VERSION=		1.6.3
DISTNAME=		pwlib-${VERSION}
CATEGORIES=		net
HOMEPAGE=		http://www.openh323.org/

# MPL 
PERMIT_PACKAGE_CDROM=	Yes
PERMIT_PACKAGE_FTP=	Yes
PERMIT_DISTFILES_CDROM= Yes
PERMIT_DISTFILES_FTP=	Yes

MASTER_SITES=		http://www.gnomemeeting.org/admin/downloads/latest/sources/sources/ 

WRKDIST=        	${WRKDIR}/pwlib

BUILD_DEPENDS+=		::devel/bison
MODULES+=		textproc/expat

#			::databases/openldap \
#			::devel/sdl \

USE_GMAKE=		Yes
USE_CXX=		Yes
AUTOCONF_NEW=		Yes
CONFIGURE_STYLE=	autoconf
CONFIGURE_ENV=		STDCCFLAGS="-I${LOCALBASE}/include/ptlib/unix"
CONFIGURE_ARGS+=	--enable-internalregex
CONFIGURE_ARGS+=	--disable-openssl		# broken for now
CONFIGURE_ARGS+=	--disable-sdl \
			--disable-openldap
MAKE_ENV=		PWLIBDIR=${WRKDIST}
ALL_TARGET=		optshared

NO_REGRESS=     	Yes

pre-build:
	@@mkdir -p ${WRKDIST}/share/pwlib
	@@ln -sf ${WRKDIST}/make ${WRKDIST}/share/pwlib

do-install:
	${INSTALL_DATA_DIR} ${PREFIX}/include/ptclib
	${INSTALL_DATA_DIR} ${PREFIX}/include/ptlib/unix/ptlib
	${INSTALL_DATA_DIR} ${PREFIX}/share/pwlib/make
	${INSTALL_DATA} ${WRKDIST}/include/ptlib/unix/ptlib/*.{h,inl} ${PREFIX}/include/ptlib/unix/ptlib
	${INSTALL_DATA} ${WRKDIST}/include/ptclib/*.h ${PREFIX}/include/ptclib
	${INSTALL_DATA} ${WRKDIST}/include/ptlib/*.{h,inl} ${PREFIX}/include/ptlib
	${INSTALL_DATA} ${WRKDIST}/include/*.h ${PREFIX}/include
	${INSTALL_DATA} ${WRKDIST}/lib/libpt.so.${VERSION} \
	    ${PREFIX}/lib/libpt.so.${VERSION:C/\.[^.]*$//}
	${INSTALL_DATA} ${WRKDIST}/lib/libpt.a ${PREFIX}/lib
	${INSTALL_DATA} ${WRKDIST}/make/*.mak ${PREFIX}/share/pwlib/make
	${INSTALL_PROGRAM} ${WRKDIST}/tools/asnparser/obj_OpenBSD_*/asnparser ${PREFIX}/bin/asnparser
	${INSTALL_SCRIPT} ${WRKDIST}/make/ptlib-config ${PREFIX}/bin
	${INSTALL_DATA_DIR} ${PREFIX}/share/doc/pwlib
	${INSTALL_DATA} ${WRKDIST}/ReadMe.txt ${PREFIX}/share/doc/pwlib

.include <bsd.port.mk>
@


1.4
log
@• KNF, simplify, etc.
• use MODULES+=textproc/expat

not a single one of them is tested, sry Benny, but you got the faster box ☻
@
text
@d1 3
a3 1
# $MirOS: ports/devel/pwlib/Makefile,v 1.3 2006/09/14 00:08:49 tg Exp $
@


1.3
log
@clean up COMMENT and HOMEPAGE; regenerate INDEX (2761, 169 marked as broken, probably many many more) � edited by script with manual postprocessing
@
text
@d1 1
a1 1
# $MirOS: ports/devel/pwlib/Makefile,v 1.2 2006/02/09 11:11:27 tg Exp $
d3 1
a3 1
COMMENT=			cross platform C++ socket and stuff library
d19 2
a20 2
BUILD_DEPENDS=		::devel/bison \
			::textproc/expat
d32 2
a33 1
CONFIGURE_ARGS+=	--disable-sdl --disable-openldap
@


1.2
log
@Fix a few abuses:
| CONFIGURE_ENV=	CPPFLAGS="-I${LOCALBASE}/include/libpng \
|			    -I${LOCALBASE}/include -I${X11BASE}/include" \
| 			LDFLAGS="-L${LOCALBASE}/lib -L${X11BASE}/lib" \
|			PKGCONFIG_PATH=...

* use Benny's pkgconfig module
  - fixes PKGCONFIG_PATH in all places
  - adds explicit dependencies on pkgconfig where needed
* use a new png module
  - also cought a few implicit dependencies
* use a new freetype1 module in two places (more to come?)
* CPPFLAGS, LDFLAGS are in CONFIGURE_ENV anyway, so correctly
  add to them as CPPFLAGS+=<tab>foo instead
* use EXTRA_{M,X,F}AKE_FLAGS
* CC is given anyway, too
* add MAKEINFO= to gnu style

Still doesn't catch all OpenBSD abuses, but most of them.

Brokenness analysis:
* Automatically done
  - CFLAGS+=CPPFLAGS
  - CXXFLAGS+=CPPFLAGS
  - CPPFLAGS+=-I${LOCALBASE}/include
  - LDFLAGS+=-L${LOCALBASE}/lib
  - CONFIGURE_ENV, MAKE_FLAGS+=
    + LDFLAGS=LDFLAGS
    + CPPFLAGS you get the idea
    + CFLAGS
    + CXXFLAGS
    + CC
    + CXX (if USE_CXX)
* Done if USE_X11=Yes
  - CPPFLAGS+=X11BASE/include
  - LDFLAGS+=X11BASE/lib

Tell me if there are problems, but first try adding
an EXTRA_ before MAKE_FLAGS and FAKE_FLAGS.
Remember EXTRA_XAKE_FLAGS get added to both.
@
text
@d1 1
a1 1
# $MirOS: ports/devel/pwlib/Makefile,v 1.1.7.1 2005/03/18 15:45:24 tg Exp $
d3 1
a3 1
COMMENT=		"cross platform C++ socket and stuff library"
@


1.1
log
@Initial revision
@
text
@d1 1
a1 1
# $MirOS$
d33 1
a33 1
MAKE_ENV=		PWLIBDIR=${WRKDIST} LDFLAGS="-L${X11BASE}/lib"
@


1.1.7.1
log
@Import the MirPorts Framework, many files moved or renamed though, no KDE/QT
@
text
@@
