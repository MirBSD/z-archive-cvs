head	1.1;
access;
symbols;
locks; strict;
comment	@# @;


1.1
date	2006.12.22.23.33.45;	author tg;	state Exp;
branches;
next	;
commitid	100458C6B702B57A964;


desc
@@


1.1
log
@unbreak on MirOS (this sucks, I don't want to hardcode version
numbers when you can autoconf that... and there isn't even a
preprocessor symbol... this is a "minimal patch" because of the
below) and polish a little (not much that can be done... this
port needs an update and more work...) but this WFM
@
text
@$MirOS$
--- lib/getline.h.orig	Thu Jan  4 16:34:32 2001
+++ lib/getline.h	Fri Dec 22 22:59:48 2006
@@@@ -27,6 +27,17 @@@@ Foundation, Inc., 59 Temple Place - Suit
 #  endif
 # endif
 
+# ifdef __MirBSD__
+#  include <sys/param.h>
+
+/* MirOS #10 has getline, but not getdelim, which we don't need though.  */
+#  if MirBSD >= 0x09A2
+#   undef __GLIBC__
+#   define __GLIBC__ 2
+#  endif
+
+# endif
+
 /* glibc2 has these functions declared in <stdio.h>.  Avoid redeclarations.  */
 # if __GLIBC__ < 2
 
@
