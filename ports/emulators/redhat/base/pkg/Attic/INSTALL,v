head	1.4;
access;
symbols;
locks; strict;
comment	@# @;


1.4
date	2008.10.05.01.23.57;	author tg;	state dead;
branches;
next	1.3;
commitid	10048E8170C54F5D796;

1.3
date	2006.02.01.20.38.45;	author tg;	state Exp;
branches;
next	1.2;
commitid	10043E11C3B4C8B055B;

1.2
date	2005.12.27.18.49.03;	author tg;	state Exp;
branches;
next	1.1;
commitid	10043B18C9826944877;

1.1
date	2005.03.18.15.45.56;	author tg;	state Exp;
branches
	1.1.7.1;
next	;

1.1.7.1
date	2005.03.18.15.45.56;	author tg;	state Exp;
branches;
next	;


desc
@@


1.4
log
@remove redhat and debian emulation in favour of fedora emulation
while probably still not feature-complete, a definitive improvement
if something breaks, tell me and I’ll fix it ASAP of course
@
text
@#!/bin/sh
# $MirOS: ports/emulators/redhat/base/pkg/INSTALL,v 1.3 2006/02/01 20:38:45 tg Exp $
#
# Pre/post-installation setup of redhat_base

# exit on errors, use a sane path and install prefix
#
set -e
PATH=/bin:/usr/bin:/sbin:/usr/sbin
PREFIX=${PKG_PREFIX:-/usr/local}

# verify proper execution
#
if [ $# -ne 2 ]; then
    echo "usage: $0 distname { PRE-INSTALL | POST-INSTALL }" >&2
    exit 1
fi

# Verify/process the command
#
case $2 in
    PRE-INSTALL)
	if [ ! -e $PREFIX/emul/linux/.setup_done ]; then
		echo "+================================================"
		echo "| Error: the linux-common package has not been"
		echo "| set up correctly. To fix this, execute"
		echo "| \$ sudo $PREFIX/emul/linux/linuxctl setup"
		echo "+================================================"
		exit 1
	fi
	if [ x"$(sysctl -n kern.emul.linux)" != x"1" ]; then
		echo "+================================================"
		echo "| Error: the Linux emulation must be enabled in"
		echo "| order to use this port. To do so, execute"
		echo "|    \$ sudo sysctl -w kern.emul.linux=1"
		echo "| Please refer to compat_linux(8) for further"
		echo "| information on this topic."
		echo "+================================================"
		exit 1
	fi
	;;
    POST-INSTALL)
	set +e
	$PREFIX/emul/linux/linuxctl register ${PREFIX}/emul/redhat \
	    bin etc lib sbin usr var
	${PREFIX}/emul/redhat/sbin/ldconfig
	set -e
	;;
    *)
	echo "usage: $0 distname { PRE-INSTALL | POST-INSTALL }" >&2
	exit 1
	;;
esac

exit 0
@


1.3
log
@revamp compat_linux(8) support library port structure to allow for
undisturbed coexistence of more than one set of libraries and dyld
and patch the opera binary to call /dbl/ld-linux.so.2 instead
@
text
@d2 1
a2 1
# $MirOS$
@


1.2
log
@* unbreak for user
* unbreak after libtoolise (remove .la files from installed system,
  crude kludge invented by bad bad bsiegert@@)
* bump
@
text
@d2 1
a2 2
# $MirOS: ports/emulators/redhat/base/pkg/INSTALL,v 1.1.7.1 2005/03/18 15:45:56 tg Exp $
# $OpenBSD: INSTALL,v 1.4 2004/03/11 20:54:14 lebel Exp $
d10 1
a10 28
PREFIX=${PREFIX:-/usr/local}/emul/redhat

# Function: tell the user what s/he needs to do to use the port just installed
#
do_notice_top()
{
    echo
    echo "+---------------"
    echo "|"
}

do_notice_link()
{
    echo "| /emul/linux already exists but it's not a symlink to $PREFIX."
    echo "|"
    echo "| To make this port active you must make /emul/linux a symlink to"
    echo "| $PREFIX."
    echo "|"
}

do_notice_bottom()
{
    echo "| Installation of $1 complete. See compat_linux(8)"
    echo "| for more information."
    echo "|"
    echo "+---------------"
    echo
}
d23 8
d35 1
a35 1
		echo "|    # sysctl -w kern.emul.linux=1"
a40 4
	set +e
	install -d -o root -g bin -m 755 /emul
	install -d -o root -g bin -m 755 $PREFIX
	set -e
a42 10
	do_notice_top

	if [ -e /emul/linux ]; then
	    if [ x`readlink /emul/linux` != x"$PREFIX" ]; then
	    	do_notice_link
	    fi
	else
	    ln -sf $PREFIX /emul/linux || do_notice_link
	fi

d44 3
a46 1
	${PREFIX}/sbin/ldconfig
a47 2

	do_notice_bottom $1
@


1.1
log
@Initial revision
@
text
@d2 1
a2 1
# $MirOS$
d61 1
d64 1
d74 1
a74 1
	    ln -sf $PREFIX /emul/linux
d77 1
d79 1
@


1.1.7.1
log
@Import the MirPorts Framework, many files moved or renamed though, no KDE/QT
@
text
@@
