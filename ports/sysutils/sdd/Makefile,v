head	1.5;
access;
symbols;
locks; strict;
comment	@# @;


1.5
date	2009.10.24.23.01.13;	author tg;	state Exp;
branches;
next	1.4;
commitid	1004AE38742457D9410;

1.4
date	2009.08.16.16.28.30;	author tg;	state Exp;
branches;
next	1.3;
commitid	1004A882F1F72B43308;

1.3
date	2008.10.04.19.03.43;	author tg;	state Exp;
branches;
next	1.2;
commitid	10048E7BD9A0842B398;

1.2
date	2006.09.14.00.13.43;	author tg;	state Exp;
branches;
next	1.1;
commitid	10045089C0E234154C3;

1.1
date	2005.03.18.15.52.20;	author tg;	state Exp;
branches
	1.1.7.1;
next	;

1.1.7.1
date	2005.03.18.15.52.20;	author tg;	state Exp;
branches;
next	;


desc
@@


1.5
log
@2842 (197 broken)
@
text
@# $OpenBSD: Makefile,v 1.14 2003/02/12 15:40:39 naddy Exp $

BROKEN=			this is wrong, we probably want to remove the .if below and use machine-openbsd-mpcc.rul always

COMMENT=		faster and improved version of dd

DISTNAME=	sdd-1.31
CATEGORIES=	sysutils


# GPL
PERMIT_PACKAGE_CDROM=	Yes
PERMIT_PACKAGE_FTP=	Yes
PERMIT_DISTFILES_CDROM=	Yes
PERMIT_DISTFILES_FTP=	Yes

MASTER_SITES=	ftp://ftp.berlios.de/pub/sdd/

USE_GMAKE=	Yes
MAKE_ENV=	MAKEPROG="${MAKE_PROGRAM}" CCOM=${_PASS_CC:T:Q} COPTX=${CFLAGS:Q}

#.if ${MACHINE} != "i386" || ${_PASS_CC:T:Q} != "cc"
do-configure:
	@@ln -sf ${WRKSRC}/RULES/i386-openbsd-cc.rul \
		${WRKSRC}/RULES/${MACHINE}-openbsd-${_PASS_CC:T:Q}.rul
#.endif

NO_REGRESS=	Yes

do-install:
	${INSTALL_PROGRAM} ${WRKBUILD}/sdd/OBJ/${MACHINE}-openbsd-${CC}/sdd \
		${PREFIX}/bin
	${INSTALL_MAN} ${WRKSRC}/sdd/sdd.1 ${PREFIX}/man/man1

.include <bsd.port.mk>
@


1.4
log
@change
• _PASS_CC back from 「mpcc」 to 「${WRKDIR}/bin/mpcc」 (note that the
  former 「${WRKDIR:Q}/bin/mpcc」 was wrong)
• all uses of _PASS_CC to either
  ‣ ${_PASS_CC:T:Q} (when passing, e.g. for libtool – this is for use
    when ${WRKDIR}/bin is in $$PATH)
  ‣ ${_PASS_CC:Q} (when invoking, e.g. in post-build targets – only
    these I saw when mechanically doing this; Benny wants to fix the
    remaining ones; I should’ve introduced no more breakage than was)
• _PASS_CXX/mpcxx just the same

agreed bsiegert@@
@
text
@d22 1
a22 1
.if ${MACHINE} != "i386" || ${_PASS_CC:T:Q} != "cc"
d26 1
a26 1
.endif
@


1.3
log
@mass conversion from
• ${CC}
• "${CC}"
• '${CC}'
• ${CC:Q}
to ${_PASS_CC:Q} (or without :Q if it’s executed, not assigned), and
same for CXX… most of these are VERY UGLY, prone to break with ccache
and maybe even without…
@
text
@d20 1
a20 1
MAKE_ENV=	MAKEPROG="${MAKE_PROGRAM}" CCOM=${_PASS_CC:Q} COPTX=${CFLAGS:Q}
d22 1
a22 1
#.if ${MACHINE} != "i386" || ${_PASS_CC} != "cc"
d25 2
a26 2
		${WRKSRC}/RULES/${MACHINE}-openbsd-${CC}.rul
#.endif
@


1.2
log
@clean up COMMENT and HOMEPAGE; regenerate INDEX (2761, 169 marked as broken, probably many many more) � edited by script with manual postprocessing
@
text
@d3 3
a5 1
COMMENT=			faster and improved version of dd
d20 1
a20 1
MAKE_ENV=	MAKEPROG="${MAKE_PROGRAM}" CCOM="${CC}" COPTX="${CFLAGS}"
d22 1
a22 1
.if ${MACHINE} != "i386" || ${CC} != "cc"
d26 1
a26 1
.endif
@


1.1
log
@Initial revision
@
text
@d3 1
a3 1
COMMENT=	"faster and improved version of dd"
@


1.1.7.1
log
@Import the MirPorts Framework, many files moved or renamed though, no KDE/QT
@
text
@@
