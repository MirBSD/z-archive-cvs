head	1.7;
access;
symbols;
locks; strict;
comment	@# @;


1.7
date	2008.07.11.14.02.41;	author tg;	state Exp;
branches;
next	1.6;
commitid	10048776813223D8644;

1.6
date	2008.05.07.22.48.32;	author tg;	state Exp;
branches;
next	1.5;
commitid	100482231C374D9CA0B;

1.5
date	2006.09.14.00.11.20;	author tg;	state Exp;
branches;
next	1.4;
commitid	10045089C0E234154C3;

1.4
date	2006.02.09.11.12.07;	author tg;	state Exp;
branches;
next	1.3;
commitid	10043EB23217A35557B;

1.3
date	2006.02.09.09.36.05;	author tg;	state Exp;
branches;
next	1.2;
commitid	10043EB0C94610EDE76;

1.2
date	2005.07.07.21.23.50;	author bsiegert;	state Exp;
branches;
next	1.1;
commitid	66bc42cd9a7ea714;

1.1
date	2005.03.18.15.50.35;	author tg;	state Exp;
branches
	1.1.7.1;
next	;

1.1.7.1
date	2005.03.18.15.50.35;	author tg;	state Exp;
branches;
next	;


desc
@@


1.7
log
@make this at least usable again, so we can update it later
‣ distfile copied to backup master site
‣ building mksh-35-1 SRPM using it…
@
text
@# $MirOS: ports/misc/rpm/Makefile,v 1.6 2008/05/07 22:48:32 tg Exp $
# $OpenBSD: Makefile,v 1.11 2002/12/29 19:49:40 fgsch Exp $

#XXX probably i386 platform only

COMMENT=		redhat package manager
DISTNAME=		rpm-3.0.6
DASH_VER=		2
CATEGORIES=		misc archivers emulators

PERMIT_DISTFILES_FTP=	Yes
PERMIT_DISTFILES_CDROM=	Yes
PERMIT_PACKAGE_FTP=	Yes
PERMIT_PACKAGE_CDROM=	Yes

HOMEPAGE=		http://www.rpm.org/
MASTER_SITES=		ftp://ftp.rpm.org/pub/rpm/dist/rpm-3.0.x/

LIB_DEPENDS+=		bz2::archivers/bzip2 \
			popt::devel/popt

CONFIGURE_STYLE=	autogen
# Enforce check for pgp, so no build-time depend is needed
CONFIGURE_ENV+=		PGPBIN=${LOCALBASE:Q}/bin/pgp
CONFIGURE_ENV+=		varprefix=${LOCALBASE:Q}
CONFIGURE_ARGS+=	${CONFIGURE_SHARED} \
			--enable-static \
			--enable-v5-packages \
			--disable-nls

post-install:
	${INSTALL_DATA_DIR} ${PREFIX:Q}/db/rpm

.include <bsd.port.mk>
@


1.6
log
@• replace MODGNU_CONFIG_GUESS_DIRS with MODGNU_RECURSE_DIRS
• always place ${WRKSRC} itself at the end of MODGNU_RECURSE_DIRS due to
  recursion issues inside of autoconf itself
• if MODGNU_RECURSE_DIRS=${WRKSRC} stood in the Makefile, nuke it (default)
• remove dead reference to MODGNU_TWIDDLE_DIRS
@
text
@d1 1
a1 1
# $MirOS: ports/misc/rpm/Makefile,v 1.5 2006/09/14 00:11:20 tg Exp $
d4 6
a9 4
COMMENT=			redhat package manager
PKGNAME=	${DISTNAME}-1
DISTNAME=	rpm-3.0.6
CATEGORIES=	misc archivers emulators
d16 2
a17 5
HOMEPAGE=	http://www.rpm.org/
MASTER_SITES=	ftp://ftp.rpm.org/pub/rpm/dist/rpm-3.0.x/

LIB_DEPENDS+=	bz2.::archivers/bzip2 \
		popt.0.::devel/popt
d19 2
a20 2
CONFIGURE_STYLE=	autoconf
MODGNU_RECURSE_DIRS=	${WRKSRC}/popt ${WRKSRC}
d22 1
d24 6
a29 4
CONFIGURE_ENV=		PGPBIN=$(LOCALBASE)/bin/pgp
CONFIGURE_ENV+=		varprefix=/var

MAKE_FLAGS=	librpm_la_LDFLAGS='-version-info 0:1:0'
d31 2
@


1.5
log
@clean up COMMENT and HOMEPAGE; regenerate INDEX (2761, 169 marked as broken, probably many many more) � edited by script with manual postprocessing
@
text
@d1 1
a1 1
# $MirOS: ports/misc/rpm/Makefile,v 1.4 2006/02/09 11:12:07 tg Exp $
d21 1
a21 1
MODGNU_CONFIG_GUESS_DIRS= ${WRKSRC} ${WRKSRC}/popt
@


1.4
log
@Fix a few abuses:
| CONFIGURE_ENV=	CPPFLAGS="-I${LOCALBASE}/include/libpng \
|			    -I${LOCALBASE}/include -I${X11BASE}/include" \
| 			LDFLAGS="-L${LOCALBASE}/lib -L${X11BASE}/lib" \
|			PKGCONFIG_PATH=...

* use Benny's pkgconfig module
  - fixes PKGCONFIG_PATH in all places
  - adds explicit dependencies on pkgconfig where needed
* use a new png module
  - also cought a few implicit dependencies
* use a new freetype1 module in two places (more to come?)
* CPPFLAGS, LDFLAGS are in CONFIGURE_ENV anyway, so correctly
  add to them as CPPFLAGS+=<tab>foo instead
* use EXTRA_{M,X,F}AKE_FLAGS
* CC is given anyway, too
* add MAKEINFO= to gnu style

Still doesn't catch all OpenBSD abuses, but most of them.

Brokenness analysis:
* Automatically done
  - CFLAGS+=CPPFLAGS
  - CXXFLAGS+=CPPFLAGS
  - CPPFLAGS+=-I${LOCALBASE}/include
  - LDFLAGS+=-L${LOCALBASE}/lib
  - CONFIGURE_ENV, MAKE_FLAGS+=
    + LDFLAGS=LDFLAGS
    + CPPFLAGS you get the idea
    + CFLAGS
    + CXXFLAGS
    + CC
    + CXX (if USE_CXX)
* Done if USE_X11=Yes
  - CPPFLAGS+=X11BASE/include
  - LDFLAGS+=X11BASE/lib

Tell me if there are problems, but first try adding
an EXTRA_ before MAKE_FLAGS and FAKE_FLAGS.
Remember EXTRA_XAKE_FLAGS get added to both.
@
text
@d1 1
a1 1
# $MirOS: ports/misc/rpm/Makefile,v 1.3 2006/02/09 09:36:05 tg Exp $
d4 1
a4 1
COMMENT=	"redhat package manager"
@


1.3
log
@remove SEPARATE_BUILD for once and for good, I'm fed up with
all the trouble it can cause - ports which need it (e.g. the
GCC SC doesnt "support" building gcc with srcdir=objdir) can
set stuff like WRKBUILD themselves.
@
text
@d1 1
a1 1
# $MirOS: ports/misc/rpm/Makefile,v 1.2 2005/07/07 21:23:50 bsiegert Exp $
a24 2
CONFIGURE_ENV+=		CPPFLAGS=-I${LOCALBASE}/include
CONFIGURE_ENV+=		LDFLAGS=-L${LOCALBASE}/lib
@


1.2
log
@Semi-automatic update of the remaining ports to the new patchlevel format.

Please correct mistakes if you find them :).
@
text
@d1 1
a1 1
# $MirOS$
a30 1
SEPARATE_BUILD=		simple
@


1.1
log
@Initial revision
@
text
@d5 1
a5 1
PKGNAME=	${DISTNAME}p1
@


1.1.7.1
log
@Import the MirPorts Framework, many files moved or renamed though, no KDE/QT
@
text
@@
