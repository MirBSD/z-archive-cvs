head	1.1;
access;
symbols;
locks; strict;
comment	@# @;


1.1
date	2009.02.22.19.08.50;	author tg;	state Exp;
branches;
next	;
commitid	10049A1A2BE2EBC0ADA;


desc
@@


1.1
log
@copy from cprng(8) – let’s see if this mode of running is feasible,
or if we wouldn’t rather use getopt(3) and more standard linkage
@
text
@# $MirOS: src/libexec/cprng/Makefile,v 1.4 2008/12/27 22:15:30 tg Exp $

PROG=		tpmrng
MAN=		tpmrng.8
LDSTATIC=	-static
LDFLAGS+=	-Wl,-n -Wl,-s

${PROG}: ${CRTI} ${CRTBEGIN} ${LIBCRT0} ${OBJS} ${LIBC} ${DPADD} ${CRTEND} ${CRTN}
	${LINK.prog} -o $@@
	${STRIP} --strip-unneeded -s -R .comment ${PROG}

.include <bsd.prog.mk>
@
